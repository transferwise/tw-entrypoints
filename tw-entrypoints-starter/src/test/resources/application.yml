spring:
  flyway:
    locations: classpath:db/migration/mariadb

tw-entrypoints:
  tas:
    sql-parser:
      cache-size-mib: 1

---
embedded:
  mysql:
    port: 3306
spring:
  config:
    activate:
      on-profile: continuous-integration
